<UserControl x:Class="Challenge_TrabalhoFuturo.TelaCadastro"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Margin="20">
        <Label Content="📋 Cadastro do Profissional (PCD)" FontSize="18" FontWeight="Bold"/>
        <TextBlock Text="Preencha os dados do colaborador para simular as necessidades de adaptação." Margin="0,5,0,15"/>

        <Grid Margin="0,10" HorizontalAlignment="Left">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="250"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Grid.Column="0" Content="Nome:" VerticalAlignment="Center"/>
            <TextBox x:Name="txtNome" Grid.Row="0" Grid.Column="1" Margin="5" Padding="5"/>

            <Label Grid.Row="1" Grid.Column="0" Content="Cargo:" VerticalAlignment="Center"/>
            <TextBox x:Name="txtCargo" Grid.Row="1" Grid.Column="1" Margin="5" Padding="5"/>

            <Label Grid.Row="2" Grid.Column="0" Content="Idade:" VerticalAlignment="Center"/>
            <TextBox x:Name="txtIdade" Grid.Row="2" Grid.Column="1" Margin="5" Padding="5"/>

            <Label Grid.Row="3" Grid.Column="0" Content="Tipo de Deficiência:" VerticalAlignment="Center"/>
            <ComboBox x:Name="cmbDeficiencia" Grid.Row="3" Grid.Column="1" Margin="5" Padding="5" SelectedIndex="0"/>
        </Grid>

        <Button Content="💾 Salvar Colaborador" Click="SalvarColaborador_Click" Margin="0,20,0,0" Padding="10" Width="250" HorizontalAlignment="Left"/>

        <TextBlock x:Name="txtFeedback" Margin="0,15,0,0" Foreground="Green" FontWeight="Bold"/>
    </StackPanel>
</UserControl>